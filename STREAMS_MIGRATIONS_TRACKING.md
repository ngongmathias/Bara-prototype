# Streams Migrations Tracking

Every `supabase/migrations/*.sql` file added by a Streams phase PR (see `STREAMS_MASTER_PLAN.md`) is logged here as it's added. None of these are applied automatically — per standing project convention, migrations are listed for review and applied separately, never via `supabase db push`. A dedicated later phase will review this list, resolve any ordering/overlap issues, and apply everything in one pass rather than one-off per phase.

**Process:** whenever a phase branch adds a migration file, add a row below in the same commit.

**Update 2026-08-05:** `20260509_ebooks.sql` (pre-existing, predates this multi-phase project) was discovered unapplied while seeding ebooks data for Phase 9 — the live DB had no `ebooks` table or bucket despite the frontend being coded against it since May. That plus rows 1–8 below were all applied by the user on 2026-08-05 and verified (schema + RPCs queried directly via the Supabase REST API, and the Phase 9 seed script ran successfully end-to-end against the live `ebooks` table/bucket afterward). `podcasts`/`podcast_episodes`/`movies`/`movie_watchlist` (from `20260319_sprint7_test_data.sql`) were confirmed already live before this pass.

**Update 2026-08-06:** Rows 10–13 were applied by the user. Applying row 10 (`20260806_security_hardening.sql`) first failed with `42703: column p.is_collaborative does not exist` — this surfaced that `20260415_collaborative_playlists.sql` (pre-existing, predates this multi-phase project, not tracked in the numbered list below) had also never been applied to production, same "written but never run" pattern as the `20260509_ebooks.sql` discovery above. The SQL Editor's failed run rolled back cleanly (verified via `pg_policies` before retrying — no partial apply). Fixed by running `20260415_collaborative_playlists.sql` first (fully idempotent, safe against any prior partial state), then rows 10–13 all applied successfully in order. **Not yet done:** the manual QA checklist (`PHASE_13_SECURITY_HARDENING.md` §3 / `PHASE_14_LAUNCH_QA.md` §3) — deferred by the user, still outstanding.

| # | Phase | File | Purpose | Applied? |
|---|-------|------|---------|----------|
| 1 | 4 | `20260805_harden_play_counting.sql` | Replaces `increment_play_count` with `record_play(song_id, device_id)` — a SECURITY DEFINER RPC that verifies caller identity from the Clerk JWT, dedupes plays per (actor, song) within 60s, and revokes the direct `songs.plays` UPDATE / `play_history` INSERT grants anon and authenticated clients previously had. | Yes (2026-08-05) |
| 2 | 5 | `20260804_consolidate_song_artists_junction.sql` | Brings `song_artists` (+ `movies.producers/writers/actors`, `songs.producer/songwriter`) into tracked migration history. Previously only existed as a hand-run script at `database/migrations/add_movie_crew_song_credits.sql` (now deleted) — fully idempotent, safe no-op wherever already applied by hand. | Yes (2026-08-05) |
| 3 | 5 | `20260804_songs_draft_publish_state.sql` | Adds `songs.status` ('draft'\|'published', default 'published') and `songs.release_date` (nullable) so artists can save drafts and schedule releases. Purely additive with a backward-compatible default — existing songs are unaffected. Enforcement of "hide drafts/future-dated songs from the public" is done client-side (`src/lib/publishFilter.ts`), not via RLS. | Yes (2026-08-05) |
| 4 | 5 | `20260804_artist_claim_flow.sql` | Adds a UNIQUE constraint on `artists.user_id` (multiple NULLs still allowed) + `artist_claims` table + `artist_claim_review` RPC, so admin-seeded artists can be claimed via a verification-style request instead of having no path to an owner at all. | Yes (2026-08-05) |
| 5 | 6 | `20260805_content_moderation.sql` | Adds `content_reports` (locked down like `verification_requests` — no direct grants, RPC-only) + `content_report_submit`/`content_reports_admin_list`/`content_report_review` RPCs. Powers the in-app Report action, the public DMCA form, and the admin moderation queue. Takedown (unpublish via `songs.status`) only works for `entity_type='song'` — depends on Phase 5's `songs.status` column (`20260804_songs_draft_publish_state.sql`). | Yes (2026-08-05) |
| 6 | 7 | `20260805_podcasts_creator_and_storage.sql` | Adds `uploaded_by`/`is_seed` to `podcasts` and `uploaded_by` to `podcast_episodes` (backfilled from parent), fixing `UserMyPodcasts.tsx`'s query against a column that never existed. Adds a UNIQUE(user_id, episode_id) constraint on `podcast_listen_history` for resume-progress upserts, plus a `record_episode_play` RPC (atomic play-count increment). Creates the `podcasts` storage bucket (100MB, audio+image MIME types) with public-read/authenticated-write policies mirroring the `ebooks` bucket. | Yes (2026-08-05) |
| 7 | 8 | `20260805_movies_watch_and_storage.sql` | Adds `movies.stream_url` — the last missing column of the `AdminMovies.tsx` schema mismatch (`producers`/`writers`/`actors` were already added by row 2; `stream_url` never was, so every admin create/edit has been failing). Adds `movie_watch_progress` (resume + "Continue Watching", same shape as `podcast_listen_history`). Creates the `movies` storage bucket (2GB limit for full films, video+image MIME types) with public-read/authenticated-write policies. | Yes (2026-08-05) |
| 8 | 9 | `20260805_ebooks_reader_and_reads.sql` | Renames `ebooks.download_count` → `read_count` (D7 rules out raw downloads — books are read in-app only, so the counter's old name no longer described what it measures) and its index. Adds `ebook_reading_progress` (resume for the new PDF/EPUB reader, same shape as `movie_watch_progress`) + a `record_ebook_read` RPC (atomic read-count increment, callable by anon too since free books can be read signed-out). | Yes (2026-08-05) |
| 9 | 11 | `20260805_admin_command_center.sql` | Adds `is_seed` to `movies`/`songs`/`artists`/`albums` (podcasts already got it in Phase 7), backfilled `true` by exact seed-migration UUID prefix (`a1000000-`/`b1000000-`/`c1000000-`/`mv100000-` — can't false-positive against real `gen_random_uuid()` rows). Adds `log_admin_action` RPC so admin destructive actions can actually write to the pre-existing `admin_activity_log` table — its only INSERT policy was `TO service_role`, which the client (authenticated as `authenticated`) could never satisfy, so nothing had ever been logged. | No |
| 10 | 13 | `20260806_security_hardening.sql` | **Pre-launch security gate (§K1/§K3/§K5/§K6).** Live pen-test found `artists`/`albums`/`songs`/`playlists`/`playlist_songs`/`song_artists` fully writable by the anon key with no login — every write-command RLS policy on these tables was `USING(true)`, and several migrations directly GRANTed anon INSERT/UPDATE/DELETE on top of that. Replaces all of it with owner-or-active-admin policies (ownership traced through `artists.user_id`/`playlists.created_by`/`podcasts.uploaded_by`), fixes the separate `auth.uid()`-vs-Clerk-TEXT-id bug on `user_song_likes`/`user_playlist_likes` (+ bonus: `user_artist_follows`, unused today, same bug), adds admin-only policies to `movies`, and REVOKEs the anon write grants outright. Adds two triggers closing a column-level escalation the ownership check alone wouldn't catch: `artists.is_verified` and `songs.featured_badge` can now only be flipped by an active admin, never via the owner's own otherwise-legitimate UPDATE. **Also fixes §K3 for real:** live-verified that `20260805_harden_play_counting.sql`'s `REVOKE UPDATE (plays)` never actually took effect — a standing table-wide UPDATE grant from `20260619_streams_songs_write_rls.sql` made the column-level revoke a no-op (confirmed via a live anon PostgREST call that successfully overwrote a real song's `plays`, then reverted it). Fixed by revoking table-wide UPDATE on `songs` and re-granting it on an explicit column list that excludes `plays`. Depends on `20260415_collaborative_playlists.sql` being applied first (see 2026-08-06 update note above). | Yes (2026-08-06) |
| 11 | 13 | `20260806_music_bucket_hardening.sql` | **§K2.** The `music` storage bucket's write policies were `TO anon, authenticated` with no path scoping at all (the migration that created it, `20260619_streams_music_bucket.sql`, said as much in its own "HARDENING FOLLOW-UP" comment). Replaces INSERT/UPDATE/DELETE with an owner-or-admin check: every creator upload path already writes under `<folder>/<own-user-id>/...`, so the policy checks that the object path's second segment matches the caller's Clerk id, OR that the caller is an active admin (admin uploads use flat, unscoped paths). Public read is untouched. Redefines the same `clerk_user_id()`/`is_active_admin()` helpers from row 10 so this file has no ordering dependency on it. | Yes (2026-08-06) |
| 12 | 14 | `20260806_songs_boost_columns.sql` | Adds `songs.is_premium`/`songs.boosted_until` — `ArtistDashboard.tsx` has always queried both for the Track Boost feature, but neither column ever existed (confirmed live: `42703 column does not exist`). Every artist's "My Songs" tab has been rendering empty (error silently swallowed into `data \|\| []`) and Track Boost has been dead. Also grants `UPDATE` on just these two columns to `authenticated`, self-contained rather than editing row 10's explicit column-grant list, to avoid a same-day apply-order dependency. Found during Phase 14 launch QA. | Yes (2026-08-06) |
| 13 | 14 | `20260806_music_covers_bucket.sql` | Creates the `music-covers` storage bucket — confirmed via live `storage.listBuckets()` that it has never existed, despite `AdminAlbums.tsx` uploading album covers to it since that page was written. Admin-write/public-read (no owner-scoped path convention here; only admins use this bucket, creator-facing cover uploads go through the `music` bucket instead). Found during Phase 14 launch QA. | Yes (2026-08-06) |

## Notes (rows 1–8 above applied 2026-08-05 — kept for history/reference on future phases)

- Applied in the order listed above (filename date order already matched dependency order across phases).
- `20260805_harden_play_counting.sql` depends on `songs` and `play_history` already existing (they do, from earlier migrations) — no new dependency risk.
- `20260804_consolidate_song_artists_junction.sql` is almost certainly already applied on the live DB by hand (the table is in active use across the app) — running it is still safe (idempotent guards throughout) but double check `song_artists` row counts before/after to confirm nothing double-applies unexpectedly.
- `20260804_songs_draft_publish_state.sql`: purely additive columns, safe to apply any time. Note the two same-day Phase 5 files (`_consolidate_song_artists_junction` and `_songs_draft_publish_state`) are independent of each other and can apply in either order.
- `20260804_artist_claim_flow.sql`: **before applying**, run `SELECT user_id, COUNT(*) FROM artists WHERE user_id IS NOT NULL GROUP BY user_id HAVING COUNT(*) > 1;` — if that returns any rows, the `ADD CONSTRAINT artists_user_id_unique UNIQUE (user_id)` statement will fail and those duplicates need manual resolution (merge or null out the extras) before this migration can land.
- `20260805_content_moderation.sql` **must apply after** Phase 5's `20260804_songs_draft_publish_state.sql` — its takedown path does `UPDATE songs SET status = 'draft'`, which needs that column to exist first. Filename order already puts it after (804 < 805), but double check if phases land out of order.
- `20260805_podcasts_creator_and_storage.sql` has no ordering dependency on the other rows — `podcasts`/`podcast_episodes`/`podcast_listen_history` are pre-existing tables from `20260319_sprint7_test_data.sql`, untouched by any other row in this list. Safe to apply independently.
- `20260805_movies_watch_and_storage.sql` should apply alongside (order doesn't matter relative to) row 2 (`20260804_consolidate_song_artists_junction.sql`) — together they're what finally makes `AdminMovies.tsx` work: row 2 added `producers`/`writers`/`actors`, this one adds `stream_url`. Applying only one of the two still leaves admin movie create/edit broken.
- `20260805_ebooks_reader_and_reads.sql` depended on `public.ebooks` existing — `20260509_ebooks.sql` was applied first to satisfy that. The column rename is guarded to no-op if `read_count` already exists or `download_count` doesn't, so it's safe to re-run.
- `20260805_admin_command_center.sql` (row 9, **not yet applied**) has no dependency on rows 1–8 — safe to apply independently. The `is_seed` backfill is a plain `UPDATE ... WHERE id::text LIKE '<prefix>-%'`, safe to re-run.
- Re-check this file at the start of every new phase for anything added since the last apply pass — new phases will add new unapplied rows below this point.

## Rows 10–13 (Phases 13–14 — pre-launch security gate + launch QA bug fixes, applied 2026-08-06)

- Actual apply order used: `20260415_collaborative_playlists.sql` (prerequisite, see 2026-08-06 update note above) → row 10 → row 11 → row 12 → row 13. All five succeeded.
- If row 9 (`20260805_admin_command_center.sql`, `is_seed` columns) is applied to `songs` in the future, remember to add `is_seed` to row 10's `GRANT UPDATE (...) ON public.songs` column list — anything left off that list silently fails to update for authenticated clients, by design.
- **Still outstanding:** the manual QA checklist (`PHASE_13_SECURITY_HARDENING.md` §3 and `PHASE_14_LAUNCH_QA.md` §3) has not been run yet — deferred by the user as of 2026-08-06. In particular, re-run `node scripts/security_pentest_anon_writes.mjs` to confirm the anon-write fix took, and spot-check the Creator Dashboard "My Songs" tab + admin album-cover upload, before treating this as fully verified.
